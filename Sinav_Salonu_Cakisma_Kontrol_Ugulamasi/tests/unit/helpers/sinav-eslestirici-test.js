
import { sinavEslestirici } from 'ember-login/helpers/sinav-eslestirici';
import { module, test } from 'qunit';

module('Unit | Helper | sinav eslestirici');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sinavEslestirici([42]);
  assert.ok(result);
});

