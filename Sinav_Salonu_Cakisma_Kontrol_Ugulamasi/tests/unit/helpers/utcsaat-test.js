
import { utcsaat } from 'ember-login/helpers/utcsaat';
import { module, test } from 'qunit';

module('Unit | Helper | utcsaat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = utcsaat([42]);
  assert.ok(result);
});

