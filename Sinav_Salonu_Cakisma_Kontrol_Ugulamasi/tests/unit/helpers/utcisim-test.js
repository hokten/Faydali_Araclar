
import { utcisim } from 'ember-login/helpers/utcisim';
import { module, test } from 'qunit';

module('Unit | Helper | utcisim');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = utcisim([42]);
  assert.ok(result);
});

